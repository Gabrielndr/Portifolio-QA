{
  "name": "portfolio-qa-cypress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:allure": "cypress run --browser chrome --headless --reporter mocha-allure-reporter",
    "cy:run:ci": "cypress run --browser chrome --headless --reporter mocha-allure-reporter"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "cypress": "^14.5.1",
    "mocha-allure-reporter": "^1.4.0"
  }
}
